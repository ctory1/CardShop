// Public frontend config. This file is safe to publish.
// When the API is running locally, this keeps the default:
//   window.CARDSHOP_API_BASE_URL = "http://localhost:5000";
//
// For production (Azure), set this to your deployed API URL, for example:
//   window.CARDSHOP_API_BASE_URL = "https://your-app-name.azurewebsites.net";
//
// To run in fully offline (no-backend) mode, leave it empty:
//   window.CARDSHOP_API_BASE_URL = "";
window.CARDSHOP_API_BASE_URL = "http://localhost:5000";